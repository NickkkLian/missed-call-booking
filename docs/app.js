/* app.js — Callback Desk UI. The state machine is engine.js (the same file the n8n export embeds); replay/play both go
   through sim.js. This file only turns events into views. No network requests at all. */
(() => {
'use strict';
const SC = window.DESK_SCENARIOS, DEFAULT_CONFIG = window.DESK_CONFIG, EXPECTED = window.DESK_EXPECTED, SIM = window.DeskSim;
const PHONE = '+12025550101';
const GROUPS = [['Happy path', ['normal_visit', 'normal_callback']], ['Duplicates & timing', ['duplicate_missed_calls', 'open_conversation_after_duplicate_window', 'no_response_one_reminder', 'late_reminder_approval', 'outside_business_hours']], ['Consent', ['stop_before_send', 'stop_cancels_reminder', 'stop_before_booking_approval', 'stale_stop_still_wins']], ['Validation', ['plain_text_requires_structured_intake', 'invalid_postal', 'invalid_address', 'outside_service_area']], ['Staff & security', ['staff_escalation', 'stale_approval_after_edit', 'replayed_staff_approval', 'forged_customer_approval', 'negative_staff_approval', 'unapproved_drafts_do_not_send']], ['Calendar', ['calendar_failure']]];
const PROVES = { normal_visit: 'missed call → SMS → WhatsApp details → staff approve → calendar → confirmation', normal_callback: 'same flow with preference = callback (address still required)', duplicate_missed_calls: 'two calls within 5 min → one draft', open_conversation_after_duplicate_window: 'call after the window but conversation open → retained, no new draft', no_response_one_reminder: '30 min → one reminder draft; 90 min → closed', late_reminder_approval: 'approving an expired reminder cannot send', outside_business_hours: 'first draft says staff will review when open', stop_before_send: 'STOP before the first send → nothing sent, later calls suppressed', stop_cancels_reminder: 'STOP cancels a queued reminder; a later approval cannot send it', stop_before_booking_approval: 'STOP before booking approval → approval cannot write the calendar', stale_stop_still_wins: 'STOP with an old timestamp still wins over ordering', plain_text_requires_structured_intake: 'free text is logged, never interpreted as a booking', invalid_postal: 'postal format checked, request blocked, clarification drafted', invalid_address: 'address format checked', outside_service_area: 'postal prefix outside the configured list', staff_escalation: 'unconfirmed request escalates internally after 45 min', stale_approval_after_edit: 'details edited → revision 2 → old approval fails', replayed_staff_approval: 'same approval replayed → one calendar action only', forged_customer_approval: 'customer route may not send approve_booking', negative_staff_approval: 'a declined approval does not advance', unapproved_drafts_do_not_send: 'time passes; unapproved drafts never send', calendar_failure: 'adapter reports failure → no customer confirmation, staff action required' };
const STATUS = { waiting_customer: ['neutral', 'Waiting for customer'], needs_details: ['warning', 'Needs details'], awaiting_staff: ['warning', 'Awaiting staff'], calendar_pending: ['info', 'Calendar pending'], booked: ['success', 'Booked'], calendar_failed: ['danger', 'Calendar failed'], opted_out: ['neutral', '⊘ Opted out'], closed_no_reply: ['neutral', 'Closed · no reply'], new: ['neutral', 'New'] };

/* ---------- helpers ---------- */
const $ = (s, r = document) => r.querySelector(s);
const SVG_TAGS = new Set(['svg', 'rect', 'line', 'text', 'defs', 'pattern', 'path', 'g', 'title', 'circle', 'polyline']);
const h = (tag, attrs = {}, ...kids) => { const el = SVG_TAGS.has(tag) ? document.createElementNS('http://www.w3.org/2000/svg', tag) : document.createElement(tag); for (const [k, v] of Object.entries(attrs)) { if (v === null || v === undefined || v === false) continue; if (k === 'class') el.setAttribute('class', v); else if (k === 'html') el.innerHTML = v; else if (k.startsWith('on')) el.addEventListener(k.slice(2), v); else if (k === 'dataset') Object.assign(el.dataset, v); else el.setAttribute(k, v === true ? '' : v); } for (const kid of kids.flat(Infinity)) { if (kid === null || kid === undefined || kid === false) continue; el.append(kid.nodeType ? kid : document.createTextNode(String(kid))); } return el; };
const tag = (kind, text) => h('span', { class: 'tag tag-' + kind }, text);
const chip = (k, v) => h('span', { class: 'chip', translate: 'no' }, k + ' ', h('b', {}, v));
const statusTag = st => { const [k, t] = STATUS[st] || ['neutral', st || '—']; return tag(k, t); };
const tz = () => S.config.timezone || 'UTC';
const fmt = (ms, opts) => { try { return new Intl.DateTimeFormat('en-GB', { timeZone: tz(), ...opts }).format(new Date(ms)); } catch (e) { return new Date(ms).toISOString(); } };
const fmtTime = ms => fmt(ms, { weekday: 'short', hour: '2-digit', minute: '2-digit', hourCycle: 'h23' });
const fmtDay = ms => fmt(ms, { day: '2-digit', month: 'short' });
const mins = (a, b) => Math.round((a - b) / 60000);
const rel = (due, now) => { const m = mins(due, now); return m >= 0 ? `in ${m}m` : `${-m}m ago`; };
const route = () => { const raw = location.hash.replace(/^#\/?/, ''); const [path, qs] = raw.split('?'); return { view: path || 'console', p: new URLSearchParams(qs || '') }; };
const go = (view, params = {}) => { const p = new URLSearchParams(); for (const [k, v] of Object.entries(params)) if (v !== null && v !== undefined && v !== '') p.set(k, v); location.hash = '#/' + view + (p.toString() ? '?' + p : ''); };
function toast(msg, { ms, kind } = {}) { const box = $('#toasts'); const el = h('div', { class: 'toast enter', role: kind === 'error' ? 'alert' : 'status' }, h('span', {}, msg), h('button', { class: 'btn btn-ghost btn-sm', 'aria-label': 'Dismiss', onclick: () => el.remove() }, '×')); box.append(el); requestAnimationFrame(() => el.classList.remove('enter')); while (box.children.length > 3) box.firstChild.remove(); setTimeout(() => { el.classList.add('leave'); setTimeout(() => el.remove(), 200); }, ms || 4000); }
function dialog(title, body, { ok = 'OK', cancel = 'Cancel', danger = false } = {}) { return new Promise(res => { const d = $('#dlg'); d.innerHTML = ''; const opener = document.activeElement; const form = h('form', { method: 'dialog' }, h('h2', {}, title), typeof body === 'string' ? h('p', { class: 'muted' }, body) : body, h('div', { class: 'acts' }, cancel && h('button', { class: 'btn', value: 'cancel', type: 'submit' }, cancel), h('button', { class: 'btn ' + (danger ? 'btn-danger' : 'btn-primary'), value: 'ok', type: 'submit' }, ok))); d.append(form); d.addEventListener('close', () => { res(d.returnValue === 'ok'); opener && opener.focus && opener.focus(); }, { once: true }); d.showModal(); }); }

/* ---------- state ---------- */
const S = { config: JSON.parse(JSON.stringify(DEFAULT_CONFIG)), pending: null, mode: 'replay', scenario: SC[0].name, step: 0, play: { events: [], now: Date.UTC(2025, 8, 15, 9, 41), seq: 0 }, ui: { chan: 'sms', mtab: 'customer', calSel: null, seenStart: false, filter: 'all' }, results: null };
try { S.ui.seenStart = localStorage.getItem('desk-seen-start') === '1'; } catch (e) {}
function runAllChecks() { S.results = SC.map((s, i) => SIM.checkScenario(s, DEFAULT_CONFIG, EXPECTED[i])); return S.results; }
const scenario = () => SC.find(s => s.name === S.scenario) || SC[0];
function current() { // events + sim for the current mode
  if (S.mode === 'replay') { const s = scenario(); const evs = s.events.slice(0, S.step); return { events: evs, all: s.events, sim: SIM.simulate(s.events, DEFAULT_CONFIG, S.step), now: evs.length ? Date.parse(evs[evs.length - 1].body.at) : Date.parse(s.events[0].body.at) - 60000, config: DEFAULT_CONFIG }; }
  return { events: S.play.events, all: S.play.events, sim: SIM.simulate(S.play.events, S.config), now: S.play.now, config: S.config };
}
const contact = sim => (sim.state.contacts || {})[PHONE] || null;
function say(entry) { const b = entry.body; switch (b.type) {
  case 'missed_call': return 'the customer calls and nobody picks up';
  case 'approve_message': return b.approved === true ? `staff approve draft ${b.messageId}` : `staff reject draft ${b.messageId}`;
  case 'details': return `the customer sends the structured form (${b.details?.preference || 'visit'}, ${b.details?.postal || ''})`;
  case 'approve_booking': return b.approved === true ? `${entry.channel === 'customer' ? 'the CUSTOMER route tries to send' : 'staff send'} approval for revision ${b.revision}, ${b.start?.slice(11, 16)}–${b.end?.slice(11, 16)}` : 'staff decline the booking';
  case 'calendar_result': return b.success ? `the calendar adapter confirms the write (${b.providerEventId})` : 'the calendar adapter reports a failed write';
  case 'message': return String(b.text || '').trim().toUpperCase() === 'STOP' ? 'the customer texts STOP' : 'the customer texts free text';
  case 'tick': return `the clock ticks (${fmtTime(Date.parse(b.at))})`;
  default: return b.type; } }

/* ---------- play-mode event factory ---------- */
function emit(type, extra = {}, channel = 'customer', at) { const id = `${type}-${++S.play.seq}`; const ev = { channel, body: { id, at: at || new Date(S.play.now).toISOString(), phone: PHONE, type, ...extra } }; S.play.events.push(ev); render(); return ev; }
function advance(m) { S.play.now += m * 60000; emit('tick', {}, 'timer'); toast(`Clock +${m}m → ${fmtTime(S.play.now)}`, { ms: 2500 }); }
async function detailsForm() {
  const d = { name: 'Test Customer 01', issue: 'Synthetic leaking fixture', address: '123 Example Road', postal: 'K1A 0B1', preference: 'visit' };
  const f = {}; const field = (k, label, attrs = {}) => h('label', { class: 'field' }, label, f[k] = h('input', { type: 'text', value: d[k], ...attrs }));
  const pref = h('select', {}, ['visit', 'callback'].map(x => h('option', { value: x }, x))); f.preference = pref;
  const body = h('div', { class: 'stack', style: 'gap:8px' }, field('name', 'name'), field('issue', 'issue'), field('address', 'address (number + street)'), field('postal', 'postal code', { autocapitalize: 'characters' }), h('label', { class: 'field' }, 'preference', pref), h('p', { class: 'muted', style: 'font-size:var(--text-xs)' }, 'Fictional customer; K1A 0B1 is a public format example. Try postal 000 or H0H 0H0 to see validation.'));
  if (!(await dialog('Send the WhatsApp intake form', body, { ok: 'Send details' }))) return;
  emit('details', { details: { name: f.name.value, issue: f.issue.value, address: f.address.value, postal: f.postal.value, preference: f.preference.value } });
}
async function freeText() { const inp = h('input', { type: 'text', value: 'Please book a visit tomorrow for the leaking fixture at 123 Example Road.', style: 'width:100%' }); if (!(await dialog('Send a free-text message', h('div', {}, inp, h('p', { class: 'muted', style: 'font-size:var(--text-xs);margin-top:8px' }, 'Free text is logged with an internal instruction; it is never interpreted as a booking.')), { ok: 'Send' }))) return; emit('message', { text: inp.value }); }

/* ---------- console ---------- */
function renderSubbar() {
  const bar = $('#subbar'); bar.innerHTML = ''; const { view } = route(); if (view !== 'console') { bar.hidden = true; return; } bar.hidden = false;
  const sel = h('select', { 'aria-label': 'Scenario', onchange: e => { const name = e.target.value; if (S.mode === 'play' && S.play.events.length) dialog('Discard this conversation?', 'Switching scenario resets the play session.', { ok: 'Discard', danger: true }).then(ok => { if (ok) { resetPlay(); S.scenario = name; S.step = 0; go('console', { scenario: name, mode: S.mode, step: 0 }); } else render(); }); else { S.scenario = name; S.step = 0; go('console', { scenario: name, mode: S.mode, step: 0 }); } } },
    GROUPS.map(([g, names]) => h('optgroup', { label: g }, names.map(n => { const i = SC.findIndex(s => s.name === n); const r = S.results?.[i]; return h('option', { value: n, selected: n === S.scenario }, `${i + 1} · ${n.replace(/_/g, ' ')}${r && !r.ok ? ' ✗' : ''}`); }))));
  const seg = h('div', { class: 'seg', style: 'margin:0', role: 'radiogroup', 'aria-label': 'Mode' }, [['replay', 'Replay'], ['play', 'Play as customer & staff']].map(([m, l]) => h('button', { role: 'radio', 'aria-checked': S.mode === m ? 'true' : 'false', onclick: () => { S.mode = m; go('console', { scenario: S.scenario, mode: m, step: S.step }); } }, l)));
  const pass = S.results.filter(r => r.ok).length;
  bar.append(...[h('span', { class: 'muted', style: 'font-size:var(--text-xs)' }, 'Scenario'), sel, seg, h('span', { class: 'clock' }, '⏱ ' + fmtTime(current().now) + ' ' + tz()),
    S.mode === 'play' ? h('span', { class: 'row', style: 'gap:4px' }, [5, 30, 45, 90].map(m => h('button', { class: 'btn btn-sm', onclick: () => advance(m) }, `+${m}m`))) : null,
    h('span', { class: 'spacer' }), h('a', { class: 'btn btn-ghost btn-sm', href: '#/scenarios', style: pass < SC.length ? 'color:var(--danger)' : '' }, `${pass}/${SC.length} scenarios pass in this tab`)].filter(Boolean));
}
function loadExample() { resetPlay(); const evs = JSON.parse(JSON.stringify(SC[0].events)); S.play.events = evs; S.play.seq = evs.length; S.play.now = Date.parse(evs[evs.length - 1].body.at) + 60000; S.play.events.push({ channel: 'customer', body: { id: 'forged-' + (++S.play.seq), at: new Date(S.play.now).toISOString(), phone: PHONE, type: 'approve_booking', approved: true, revision: 1, start: '2025-09-17T10:00:00Z', end: '2025-09-17T11:00:00Z', actor: 'staff' } }); S.mode = 'play'; go('console', { scenario: S.scenario, mode: 'play', step: S.step }); render(); }
function resetPlay() { S.play = { events: [], now: Date.UTC(2025, 8, 15, 9, 41), seq: 0 }; S.ui.calSel = null; }
function viewConsole(main) {
  const cur = current(), sim = cur.sim, c = contact(sim), now = cur.now, cfg = cur.config, play = S.mode === 'play';
  main.append(h('h1', { class: 'sr-only' }, 'Console'));
  main.append(h('div', { class: 'mtabs', role: 'tablist' }, [['customer', 'Customer'], ['staff', 'Staff desk'], ['calendar', 'Calendar & state']].map(([k, l]) => h('button', { role: 'tab', 'aria-selected': S.ui.mtab === k ? 'true' : 'false', onclick: () => { S.ui.mtab = k; render(); } }, l))));
  const grid = h('div', { class: 'console' });
  // ── customer column
  const msgs = c ? c.messages : [];
  const thread = h('div', { class: 'thread', role: 'log', 'aria-live': 'polite' });
  const items = [];
  for (const e of cur.events) { const b = e.body; if (b.type === 'missed_call') items.push({ at: Date.parse(b.at), el: h('div', { class: 'bubble is-system' }, `missed call · ${PHONE} · ${fmtTime(Date.parse(b.at))}`) }); if (b.type === 'message') items.push({ at: Date.parse(b.at), chan: 'whatsapp', el: h('div', { class: 'bubble from-customer' }, h('div', { class: 'channel' }, 'WhatsApp'), b.text, h('time', {}, fmtTime(Date.parse(b.at)))) }); if (b.type === 'details') items.push({ at: Date.parse(b.at), chan: 'whatsapp', el: h('div', { class: 'bubble from-customer' }, h('div', { class: 'channel' }, 'WhatsApp · intake form'), `${b.details.name} · ${b.details.issue} · ${b.details.address} · ${b.details.postal} · ${b.details.preference}`, h('time', {}, fmtTime(Date.parse(b.at)))) }); }
  for (const m of msgs) { const chan = m.purpose === 'initial' ? 'sms' : 'whatsapp'; const sent = m.status === 'sent_simulated'; const cls = sent ? 'from-business' : m.status === 'rejected' ? 'is-rejected' : 'is-draft'; const label = `${chan} · ${sent ? 'sent (simulated)' : m.status === 'cancelled' ? 'draft cancelled' : m.status === 'rejected' ? 'rejected by staff' : 'draft, awaiting staff approval'}`; items.push({ at: sent ? Date.parse(m.approvedAt) : m.createdAt, chan, el: h('div', { class: 'bubble ' + cls }, h('div', { class: 'channel' }, label), m.text, h('time', {}, chip('msg', m.id), ' ', fmtTime(sent ? Date.parse(m.approvedAt) : m.createdAt))) }); }
  items.sort((a, b) => a.at - b.at);
  const shown = items.filter(x => !x.chan || x.chan === S.ui.chan);
  if (!shown.length) thread.append(h('div', { class: 'bubble is-system' }, S.ui.chan === 'sms' ? (c ? 'the SMS is waiting for staff approval →' : 'nothing yet — start with a missed call') : 'nothing on WhatsApp yet'));
  else shown.forEach(x => thread.append(x.el));
  const counts = { sms: items.filter(x => x.chan === 'sms').length, whatsapp: items.filter(x => x.chan === 'whatsapp').length };
  const phone = h('div', { class: 'phone' }, h('div', { class: 'ph-head' }, h('b', {}, PHONE), h('span', { class: 'muted' }, 'Test Customer 01'), h('span', { class: 'spacer' }), c ? statusTag(c.status) : null),
    h('div', { class: 'chan-tabs', role: 'tablist' }, ['sms', 'whatsapp'].map(ch => h('button', { role: 'tab', 'aria-selected': S.ui.chan === ch ? 'true' : 'false', onclick: () => { S.ui.chan = ch; render(); } }, ch.toUpperCase(), h('span', { class: 'n' }, counts[ch])))), thread);
  if (play) phone.append(h('div', { class: 'composer' }, h('div', { class: 'row' }, h('button', { class: 'btn btn-sm', onclick: () => emit('missed_call') }, 'Call again'), S.play.events.length ? null : h('button', { class: 'btn btn-sm btn-ghost', title: 'Seed this session with the normal_visit conversation plus one forged approval', onclick: loadExample }, 'Load example conversation'), h('button', { class: 'btn btn-sm', onclick: freeText }, 'Message…'), h('button', { class: 'btn btn-sm', onclick: detailsForm }, 'Send details form'), h('button', { class: 'btn btn-sm', onclick: () => emit('message', { text: ' STOP ' }) }, 'Send STOP')),
    h('details', {}, h('summary', { class: 'muted', style: 'font-size:var(--text-xs);cursor:pointer' }, 'Edge cases ▾ (what the guard is for)'), h('div', { class: 'row', style: 'margin-top:8px' },
      h('button', { class: 'btn btn-sm', title: 'approve_booking sent on the customer route', onclick: () => emit('approve_booking', { approved: true, revision: c?.revision || 1, start: new Date(S.play.now + 86400000).toISOString(), end: new Date(S.play.now + 90000000).toISOString(), actor: 'staff' }, 'customer') }, 'Send a forged approval'),
      h('button', { class: 'btn btn-sm', title: 'resend the last staff event with the same id', onclick: () => { const last = [...S.play.events].reverse().find(e => e.channel === 'staff'); if (!last) return toast('No staff event to replay yet'); S.play.events.push(JSON.parse(JSON.stringify(last))); render(); } }, 'Replay last staff approval'),
      h('button', { class: 'btn btn-sm', title: 'STOP with a timestamp older than the last event', onclick: () => emit('message', { text: 'STOP' }, 'customer', new Date(Date.UTC(2025, 8, 15, 9, 0)).toISOString()) }, 'STOP with an old timestamp')))));
  else { const all = cur.all; phone.append(h('div', { class: 'stepper' }, h('button', { class: 'btn btn-sm', disabled: S.step <= 0, onclick: () => setStep(S.step - 1) }, '◀ Prev'), h('span', { class: 'pos' }, `step ${S.step} / ${all.length}`), h('button', { class: 'btn btn-sm btn-primary', disabled: S.step >= all.length, onclick: () => setStep(S.step + 1) }, 'Next ▶'),
    h('span', { class: 'say' }, S.step < all.length ? 'next: ' + say(all[S.step]) : (() => { const r = S.results[SC.indexOf(scenario())]; return `end · expected: ${scenario().expected.status} · actual: ${c?.status || '—'} ${r.ok ? '✓' : '✗ ' + r.problems.join('; ')}`; })()))); }
  grid.append(h('section', { class: 'col col-customer' + (S.ui.mtab === 'customer' ? ' active' : '') }, h('h2', {}, 'Customer', h('span', { class: 'you' }, play ? 'you are: customer' : 'replay')), phone));
  // ── staff column
  const staff = h('section', { class: 'col col-staff' + (S.ui.mtab === 'staff' ? ' active' : '') }, h('h2', {}, 'Staff desk', h('span', { class: 'you' }, play ? 'you are: staff' : 'replay')));
  if (!S.ui.seenStart && S.step === 0 && !play) staff.append(h('div', { class: 'guide' }, h('b', {}, 'Start here. '), 'A customer just missed a call. Press ', h('kbd', {}, 'Next'), ' in the phone panel to let the system draft an SMS — nothing is sent until staff approve it. Switch to ', h('i', {}, 'Play'), ' to act as customer and staff yourself. ', h('button', { class: 'btn btn-sm btn-ghost', onclick: () => { S.ui.seenStart = true; try { localStorage.setItem('desk-seen-start', '1'); } catch (e) {} render(); } }, 'Got it')));
  const drafts = msgs.filter(m => m.status === 'needs_staff_approval'), sent = msgs.filter(m => m.status !== 'needs_staff_approval');
  staff.append(h('h3', { style: 'font-size:var(--text-sm);margin:8px 0 6px' }, `Message drafts (${drafts.length})`));
  if (!drafts.length) staff.append(h('p', { class: 'muted', style: 'font-size:var(--text-xs);margin-bottom:8px' }, c ? 'no draft waiting' : 'no conversation yet'));
  for (const m of drafts) staff.append(h('div', { class: 'qcard await' }, h('div', { class: 'qh' }, tag('draft', 'Draft · awaiting staff'), h('span', {}, (m.purpose === 'initial' ? 'SMS' : 'WhatsApp') + ' → ' + PHONE), chip('msg', m.id), chip('rev', m.revision), h('span', { class: 'spacer' }), h('span', { class: 'mono' }, fmtTime(m.createdAt))), h('div', { class: 'txt' }, '“' + m.text + '”'),
    h('div', { class: 'acts' }, h('button', { class: 'btn btn-sm btn-primary', disabled: !play, title: play ? '' : 'Replay mode: use Next in the phone panel', onclick: () => emit('approve_message', { messageId: m.id, approved: true }, 'staff') }, 'Approve & send'), h('button', { class: 'btn btn-sm', disabled: !play, onclick: () => emit('approve_message', { messageId: m.id, approved: false }, 'staff') }, 'Reject'))));
  for (const m of sent.slice(-3)) staff.append(h('div', { class: 'qcard', style: 'color:var(--text-2)' }, h('div', { class: 'qh' }, m.status === 'sent_simulated' ? tag('info', 'Sent · simulated') : m.status === 'rejected' ? tag('danger', 'Rejected') : tag('neutral', 'Cancelled'), chip('msg', m.id), h('span', {}, m.purpose)), h('div', { class: 'txt', style: 'font-size:var(--text-xs)' }, m.text.slice(0, 120) + (m.text.length > 120 ? '…' : ''))));
  staff.append(h('h3', { style: 'font-size:var(--text-sm);margin:12px 0 6px' }, `Booking requests (${c && ['awaiting_staff', 'calendar_pending', 'booked', 'calendar_failed'].includes(c.status) ? 1 : 0})`));
  if (c && c.details && c.status !== 'needs_details') {
    const d = c.details, cal = c.calendar; const sel = S.ui.calSel;
    const startIn = h('input', { type: 'datetime-local', 'aria-label': 'Start time', value: sel ? sel.start.slice(0, 16) : new Date(now + 86400000 - (now % 3600000)).toISOString().slice(0, 16), style: 'font-size:var(--text-xs)' }), endIn = h('input', { type: 'datetime-local', 'aria-label': 'End time', value: sel ? sel.end.slice(0, 16) : new Date(now + 90000000 - (now % 3600000)).toISOString().slice(0, 16), style: 'font-size:var(--text-xs)' });
    const card = h('div', { class: 'qcard ' + (c.status === 'awaiting_staff' ? 'await' : c.status === 'calendar_failed' ? 'blocked' : '') }, h('div', { class: 'qh' }, statusTag(c.status), chip('rev', c.revision), cal ? chip('key', cal.key) : null, c.escalated ? tag('warning', 'escalated · internal') : null),
      h('dl', {}, h('dt', {}, 'customer'), h('dd', {}, `${d.name} · ${d.preference}`), h('dt', {}, 'issue'), h('dd', {}, d.issue), h('dt', {}, 'address'), h('dd', {}, `${d.address} · ${d.postal} ✓ format ok · ✓ in service area (${d.postal.slice(0, 3)})`), cal ? [h('dt', {}, 'time'), h('dd', { class: 'mono' }, `${cal.start} → ${cal.end} · ${cal.status}${cal.providerEventId ? ' · ' + cal.providerEventId : ''}`)] : null));
    if (c.status === 'awaiting_staff') card.append(h('div', { class: 'row', style: 'font-size:var(--text-xs)' }, 'time: pick a slot in the calendar →  or ', startIn, endIn), h('div', { class: 'acts' }, h('button', { class: 'btn btn-sm btn-primary', disabled: !play, title: play ? '' : 'Replay mode', onclick: () => { const st = new Date(startIn.value + 'Z'), en = new Date(endIn.value + 'Z'); emit('approve_booking', { approved: true, revision: c.revision, start: st.toISOString().replace('.000Z', 'Z'), end: en.toISOString().replace('.000Z', 'Z') }, 'staff'); } }, `Approve revision ${c.revision} & write calendar`), h('button', { class: 'btn btn-sm', disabled: !play, onclick: () => emit('approve_booking', { approved: false, revision: c.revision }, 'staff') }, 'Decline')));
    if (c.status === 'calendar_pending') card.append(h('div', { class: 'acts' }, h('span', { class: 'muted', style: 'font-size:var(--text-xs)' }, 'waiting for the adapter receipt'), play ? [h('button', { class: 'btn btn-sm', onclick: () => emit('calendar_result', { bookingKey: cal.key, success: true, providerEventId: 'synthetic-event-' + String(S.play.seq + 1).padStart(2, '0') }, 'adapter') }, 'Simulate receipt: success'), h('button', { class: 'btn btn-sm', onclick: () => emit('calendar_result', { bookingKey: cal.key, success: false }, 'adapter') }, 'Simulate receipt: failure')] : null));
    if (c.status === 'calendar_failed') card.append(h('div', { class: 'guardline no' }, '⊘ calendar write failed · staff action required · no customer confirmation drafted'));
    staff.append(card);
  } else staff.append(h('p', { class: 'muted', style: 'font-size:var(--text-xs);margin-bottom:8px' }, c && c.status === 'needs_details' ? 'blocked: the intake needs corrections (see the clarification draft)' : 'waiting for structured details'));
  const esc = sim.log.filter(l => l.text.startsWith('STAFF ESCALATION'));
  staff.append(h('h3', { style: 'font-size:var(--text-sm);margin:12px 0 6px' }, `Escalations (${esc.length})`), esc.length ? h('div', { class: 'guardline' }, esc.map(l => `${fmtTime(Date.parse(l.at))} · ${l.text}`).join('\n')) : h('p', { class: 'muted', style: 'font-size:var(--text-xs)' }, 'none'));
  staff.append(h('h3', { style: 'font-size:var(--text-sm);margin:12px 0 6px' }, `Event log (${sim.log.length}) `, h('a', { href: '#/log', style: 'font-size:var(--text-xs);font-weight:normal' }, '→ full log')), logList(sim.log.slice(-6), now));
  grid.append(staff);
  // ── state + calendar column
  const right = h('section', { class: 'col col-cal' + (S.ui.mtab === 'calendar' ? ' active' : '') }, h('h2', {}, 'Calendar & state'));
  const lastGuard = [...sim.guards].pop(), lastNo = [...sim.log].reverse().find(l => l.kind === 'guard-no');
  const timers = c ? [['reminder', c.lastSentAt !== null && c.status === 'waiting_customer' && !c.reminderCreated ? c.lastSentAt + cfg.reminderMinutes * 60000 : null], ['stop', c.lastSentAt !== null && c.status === 'waiting_customer' ? c.lastSentAt + cfg.stopMinutes * 60000 : null], ['escalation', c.status === 'awaiting_staff' && !c.escalated ? c.requestedAt + cfg.escalateMinutes * 60000 : null]] : [];
  right.append(h('div', { class: 'card state', style: 'margin-bottom:12px' }, h('h3', {}, 'State'), h('dl', {}, h('dt', {}, 'status'), h('dd', {}, c ? statusTag(c.status) : '— no conversation'), h('dt', {}, 'revision'), h('dd', { class: 'mono' }, c ? c.revision : '—'), timers.map(([k, due]) => [h('dt', {}, k), h('dd', { class: 'mono' }, due ? `due ${fmtTime(due)} (${rel(due, now)})` : '—')]), h('dt', {}, 'opt-out'), h('dd', {}, c && c.status === 'opted_out' ? 'yes · cannot be re-enabled by later calls or approvals' : 'no'), h('dt', {}, 'drafts'), h('dd', { class: 'mono' }, `${drafts.length} waiting · ${sim.actions.length} simulated sends/writes`)),
    h('div', { style: 'margin-top:8px' }, h('div', { class: 'muted', style: 'font-size:var(--text-2xs);text-transform:uppercase;letter-spacing:.04em;margin-bottom:4px' }, 'Last guard decision'), lastNo && (!lastGuard || lastNo.i >= lastGuard.i) ? h('div', { class: 'guardline no' }, '⊘ ' + lastNo.text) : lastGuard ? h('div', { class: 'guardline' }, '✓ ' + lastGuard.text) : h('div', { class: 'guardline' }, 'no guarded action yet'))));
  right.append(calendar(c, now, cfg, play));
  grid.append(right); main.append(grid);
}
function setStep(k) { S.step = Math.max(0, Math.min(scenario().events.length, k)); go('console', { scenario: S.scenario, mode: 'replay', step: S.step }); }
function logList(entries, now) { return h('div', { class: 'loglist' }, entries.length ? entries.map(l => h('div', {}, h('span', {}, fmtTime(Date.parse(l.at)).slice(-5)), h('span', { class: 'muted' }, l.actor), h('span', { class: l.kind === 'guard-no' ? 'no' : l.kind === 'sim' ? 'sim' : '' }, (l.kind === 'guard-no' ? '⊘ ' : '') + l.text + (l.kind === 'sim' ? ' (Simulated)' : '')))) : h('div', { class: 'muted' }, 'empty')); }
function calendar(c, now, cfg, play) {
  const day = 86400000, weekStart = (() => { const d = new Date(now); const dow = (d.getUTCDay() + 6) % 7; return Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()) - dow * day; })();
  const days = [...Array(7)].map((_, i) => weekStart + i * day), hours = []; for (let hh = Math.max(0, cfg.openHour - 1); hh < Math.min(24, cfg.closeHour + 1); hh++) hours.push(hh);
  const names = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
  const cal = c?.calendar, evStart = cal ? Date.parse(cal.start) : null, evEnd = cal ? Date.parse(cal.end) : null;
  const tbl = h('table', { role: play ? 'grid' : null, 'aria-label': 'Week view' }, h('thead', {}, h('tr', {}, h('th', { scope: 'col' }, h('span', { class: 'sr-only' }, 'Hour')), days.map((d, i) => h('th', { class: Math.floor(now / day) === Math.floor(d / day) ? 'today' : '' }, `${names[i]} ${fmtDay(d).slice(0, 2)}`)))),
    h('tbody', {}, hours.map(hh => h('tr', {}, h('td', { class: 'hour' }, String(hh).padStart(2, '0')), days.map((d, i) => { const t = d + hh * 3600000, open = cfg.businessDays.includes(names[i]) && hh >= cfg.openHour && hh < cfg.closeHour, past = t <= now; let cls = open ? 'open' : ''; if (past) cls += ' past'; if (cal && t < evEnd && t + 3600000 > evStart) cls += ' ev ' + (cal.status === 'confirmed' ? '' : cal.status === 'failed' ? 'failed' : 'pending'); const sel = S.ui.calSel && S.ui.calSel.start === new Date(t).toISOString().replace('.000Z', 'Z'); if (sel) cls += ' sel';
      const td = h('td', { class: cls.trim(), title: `${names[i]} ${String(hh).padStart(2, '0')}:00${open ? '' : ' · outside business hours'}${past ? ' · past' : ''}${cal && cls.includes('ev') ? ' · ' + cal.key + ' · ' + cal.status + ' (simulated)' : ''}`, role: play && open && !past ? 'gridcell' : null, tabindex: play && open && !past ? '0' : null });
      if (play && open && !past) { const pick = () => { S.ui.calSel = { start: new Date(t).toISOString().replace('.000Z', 'Z'), end: new Date(t + 3600000).toISOString().replace('.000Z', 'Z') }; render(); toast(`Slot picked: ${names[i]} ${String(hh).padStart(2, '0')}:00–${String(hh + 1).padStart(2, '0')}:00`, { ms: 2000 }); }; td.addEventListener('click', pick); td.addEventListener('keydown', e => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); pick(); } }); }
      return td; })))));
  return h('div', { class: 'cal' }, h('div', { class: 'ch' }, h('b', {}, `Week of ${fmtDay(weekStart)} · ${tz()}`), h('span', {}, '· writes are simulated'), play ? h('span', { class: 'spacer' }) : null, play ? h('span', { class: 'faint' }, 'click a slot to pick a time') : null), h('div', { style: 'overflow-x:auto' }, tbl), h('div', { class: 'legend' }, h('span', {}, '▢ dashed = pending write'), h('span', {}, '■ = written (simulated)'), h('span', {}, '░ = outside hours / closed day'), h('span', {}, 'red = write failed')));
}

/* ---------- scenarios / config / log ---------- */
function viewScenarios(main) {
  const res = runAllChecks(), pass = res.filter(r => r.ok).length, f = S.ui.filter;
  main.append(h('div', { class: 'page-head' }, h('h1', {}, `Scenarios · ${SC.length}`), h('span', { class: 'muted' }, `all replayed in this tab just now → `, h('b', { class: 'mono', style: pass < SC.length ? 'color:var(--danger)' : 'color:var(--success)' }, `${pass}/${SC.length} match expected`)), h('span', { class: 'spacer' }), h('div', { class: 'filters', style: 'margin:0' }, ['all', 'pass', 'fail'].map(x => h('button', { 'aria-pressed': f === x ? 'true' : 'false', onclick: () => { S.ui.filter = x; render(); } }, x))), h('button', { class: 'btn btn-sm', onclick: () => { runAllChecks(); render(); toast('Re-ran all 22 scenarios'); } }, 'Re-run')));
  const list = h('div', { class: 'scen' });
  for (const [g, names] of GROUPS) { const rows = names.map(n => { const i = SC.findIndex(s => s.name === n); return [i, res[i]]; }).filter(([, r]) => f === 'all' || (f === 'pass') === r.ok); if (!rows.length) continue; list.append(h('h2', {}, `${g} (${rows.length})`)); for (const [i, r] of rows) list.append(h('div', { class: 'row' + (r.ok ? '' : ' fail') }, h('span', { style: r.ok ? 'color:var(--success)' : 'color:var(--danger)' }, r.ok ? '✓' : '✗'), h('span', { class: 'n' }, i + 1), h('span', {}, h('b', {}, SC[i].name.replace(/_/g, ' ')), ' ', h('span', { class: 'what' }, PROVES[SC[i].name] || ''), r.ok ? null : h('div', { style: 'color:var(--danger)' }, r.problems.join('; '))), statusTag(SC[i].expected.status), h('a', { class: 'btn btn-sm', href: `#/console?scenario=${SC[i].name}&mode=replay&step=0`, onclick: () => { S.scenario = SC[i].name; S.step = 0; S.mode = 'replay'; } }, 'Open'))); }
  main.append(list, h('p', { class: 'muted', style: 'font-size:var(--text-xs);margin-top:12px' }, 'Expected outcomes come from scenarios.json; the "match" check also compares final status, action counts and log length with output/transcripts.json recorded by `node demo.js build`. Groups are an editorial reading of the README failure-handling table.'));
}
function viewConfig(main) {
  const p = S.pending || JSON.parse(JSON.stringify(S.config)); const errs = [];
  if (!(p.reminderMinutes < p.stopMinutes)) errs.push('reminder must be less than stop'); for (const k of ['duplicateMinutes', 'reminderMinutes', 'stopMinutes', 'escalateMinutes']) if (!(p[k] > 0)) errs.push(k + ' must be > 0'); if (!(p.openHour < p.closeHour)) errs.push('open must be before close');
  const num = (k, label, unit) => h('label', { class: 'field' }, label, h('span', { class: 'rule-edit' }, h('input', { type: 'number', min: '0', value: p[k], style: 'width:6em', oninput: e => { S.pending = { ...p, [k]: Number(e.target.value) }; } }), unit));
  main.append(h('div', { class: 'page-head' }, h('h1', {}, 'Config'), h('span', { class: 'sub' }, 'Changes apply to Play sessions. Replays and the 22 scenario checks always use the defaults from config.json.'), h('span', { class: 'spacer' }), h('button', { class: 'btn btn-sm', onclick: () => { S.pending = JSON.parse(JSON.stringify(DEFAULT_CONFIG)); render(); } }, 'Reset to defaults')));
  const form = h('div', { class: 'config' },
    h('div', { class: 'card' }, h('h2', {}, 'Business'), h('label', { class: 'field' }, 'Business name', h('input', { type: 'text', value: p.businessName, oninput: e => { S.pending = { ...p, businessName: e.target.value }; } })), h('p', { class: 'hint' }, 'demo name — cannot be a real business'), h('div', { class: 'field' }, 'Business days', h('div', { class: 'row' }, ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map(d => h('label', { class: 'check' }, h('input', { type: 'checkbox', checked: p.businessDays.includes(d), onchange: e => { const days = p.businessDays.filter(x => x !== d); if (e.target.checked) days.push(d); S.pending = { ...p, businessDays: days }; } }), d)))), h('div', { class: 'row', style: 'margin-top:8px' }, num('openHour', 'open', ':00'), num('closeHour', 'close', ':00'), h('label', { class: 'field' }, 'timezone', h('input', { type: 'text', value: p.timezone, style: 'width:10em', oninput: e => { S.pending = { ...p, timezone: e.target.value }; } }))), h('p', { class: 'hint' }, 'outside these, first drafts say staff will review when open')),
    h('div', { class: 'card' }, h('h2', {}, 'Service area'), h('div', { class: 'row' }, p.servicePostalPrefixes.map(x => h('span', { class: 'chip' }, x, ' ', h('button', { class: 'btn btn-ghost btn-sm', 'aria-label': 'remove ' + x, onclick: () => { S.pending = { ...p, servicePostalPrefixes: p.servicePostalPrefixes.filter(y => y !== x) }; render(); } }, '×'))), h('input', { type: 'text', placeholder: 'K1A', maxlength: '3', style: 'width:5em', autocapitalize: 'characters', onkeydown: e => { if (e.key === 'Enter' && e.target.value.trim()) { S.pending = { ...p, servicePostalPrefixes: [...p.servicePostalPrefixes, e.target.value.trim().toUpperCase()] }; render(); } } })), h('p', { class: 'hint' }, 'format check only — not geocoding, not proof an address exists · Enter adds a prefix')),
    h('div', { class: 'card' }, h('h2', {}, 'Timing (minutes)'), h('div', { class: 'row' }, num('duplicateMinutes', 'Duplicate window', 'min'), num('reminderMinutes', 'Reminder after', 'min'), num('stopMinutes', 'Stop after', 'min'), num('escalateMinutes', 'Escalate after', 'min')), h('p', { class: 'hint' }, 'constraints: reminder < stop; all > 0')),
    h('div', { class: 'card ro' }, h('h2', {}, 'Adapters (read-only in this demo)'), h('dl', { class: 'kv' }, h('dt', {}, 'WhatsApp number'), h('dd', { class: 'mono' }, p.whatsappPhone, h('span', { class: 'muted' }, ' — reserved 555-01xx test range · no live account')), h('dt', {}, 'Gateway base URL'), h('dd', { class: 'mono' }, p.gatewayBaseUrl, h('span', { class: 'muted' }, ' — placeholder · no provider adapter included')), h('dt', {}, 'Dry run'), h('dd', {}, '● on (locked) — every send and calendar write is simulated'))),
    errs.length ? h('div', { class: 'error', role: 'alert' }, h('h3', {}, 'Fix before saving'), h('ul', {}, errs.map(e => h('li', {}, e)))) : null,
    h('div', { class: 'savebar' }, h('button', { class: 'btn', onclick: () => { S.pending = null; render(); } }, 'Discard'), h('button', { class: 'btn btn-primary', disabled: !!errs.length || !S.pending, onclick: () => { S.config = { ...S.pending, dryRun: true }; S.pending = null; toast('Config saved for Play sessions'); render(); } }, 'Save changes')));
  main.append(form);
}
function viewLog(main) {
  const cur = current(), f = S.ui.logActor || 'all', rows = cur.sim.log.filter(l => f === 'all' || l.actor === f || (f === 'guard' && l.kind === 'guard-no'));
  main.append(h('div', { class: 'page-head' }, h('h1', {}, 'Log'), h('span', { class: 'muted' }, `${S.mode === 'replay' ? 'scenario ' + S.scenario.replace(/_/g, ' ') + ' · step ' + S.step : 'play session'} · ${cur.sim.log.length} events`), h('span', { class: 'spacer' }), h('div', { class: 'filters', style: 'margin:0' }, ['all', 'customer', 'staff', 'timer', 'adapter', 'guard'].map(x => h('button', { 'aria-pressed': f === x ? 'true' : 'false', onclick: () => { S.ui.logActor = x; render(); } }, x))), h('button', { class: 'btn btn-sm', onclick: () => { const a = h('a', { href: URL.createObjectURL(new Blob([JSON.stringify({ mode: S.mode, scenario: S.scenario, events: cur.events, log: cur.sim.log, actions: cur.sim.actions, state: cur.sim.state }, null, 2)], { type: 'application/json' })), download: `desk-log-${S.mode === 'replay' ? S.scenario : 'play'}.json` }); document.body.append(a); a.click(); a.remove(); } }, 'Export JSON')));
  main.append(h('div', { class: 'card' }, logList(rows, cur.now)));
}

/* ---------- render / keys / init ---------- */
function help() { dialog('Keyboard shortcuts', h('table', {}, [['→ / Space', 'next step (replay)'], ['←', 'previous step'], ['?', 'this help'], ['Esc', 'close']].map(([k, v]) => h('tr', {}, h('td', {}, h('kbd', {}, k)), h('td', { class: 'muted' }, v)))), { ok: 'Close', cancel: null }); }
function render() {
  if (route().view === 'settings') { location.replace('#/config'); return; }   // old address; Settings is now the appearance dialog
  const { view, p } = route(); const main = $('#main'); main.innerHTML = '';
  if (view === 'console') { const sc = p.get('scenario'); if (sc && SC.some(s => s.name === sc)) S.scenario = sc; const m = p.get('mode'); if (m === 'play' || m === 'replay') S.mode = m; const st = parseInt(p.get('step') || '', 10); if (!Number.isNaN(st)) S.step = Math.max(0, Math.min(scenario().events.length, st)); }
  if (!S.results) runAllChecks();
  renderSubbar();
  ({ console: viewConsole, scenarios: viewScenarios, config: viewConfig, log: viewLog }[view] || viewConsole)(main);
  document.title = 'Callback Desk · ' + view[0].toUpperCase() + view.slice(1);
  const tabs = h('div', { class: 'tabs' }); main.prepend(tabs);
  tabs.append(...[['console', 'Console'], ['scenarios', 'Scenarios'], ['config', 'Config'], ['log', 'Log']].map(([v, l]) => h('a', { href: '#/' + v + (v === 'console' ? `?scenario=${S.scenario}&mode=${S.mode}&step=${S.step}` : ''), 'aria-current': view === v ? 'page' : null }, l)));
}
function keys(e) { if (e.target.closest('input, select, textarea, [contenteditable]') || document.querySelector('dialog[open]')) return; if (e.key === '?') { e.preventDefault(); help(); return; } if (route().view !== 'console' || S.mode !== 'replay') return; if (e.key === 'ArrowRight' || e.key === ' ') { e.preventDefault(); setStep(S.step + 1); } if (e.key === 'ArrowLeft') { e.preventDefault(); setStep(S.step - 1); } }
function init() {
  const root = document.documentElement, tb = $('#theme');
  Appearance.bindToggle(tb);   // ◐ switches light/dark only (appearance.js)
  Appearance.bindSettings($('#nl-settings-button'));   // the gear: palette + light/dark
  $('#help').addEventListener('click', help); document.addEventListener('keydown', keys); window.addEventListener('hashchange', render);
  let rt; window.addEventListener('resize', () => { clearTimeout(rt); rt = setTimeout(render, 150); });
  if (!location.hash) location.replace('#/console?scenario=normal_visit&mode=replay&step=0');
  if (new URLSearchParams(location.search).get('example') === '1') { loadExample(); return; }
  render();
}
window.CallbackDesk = { state: S, current, runAllChecks, emit, advance };
init();
})();
